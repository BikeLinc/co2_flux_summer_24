clc, clear, close all


%% Import Data

[file, location] = uigetfile("*.txt");

T = renamevars(T,["Var1","Var2","Var3","Var4","Var5","Var6","Var7","Var8"],...
                ["MS","Q",""])

%%
data = readtable(string(location) + string(file));